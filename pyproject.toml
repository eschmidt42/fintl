[project]
name = "fintl"
version = "0.2.2"
description = "Financial ETL CLI: parse, normalize, and explore your bank transaction data."
readme = "README.md"
license = { text = "MIT" }
keywords = ["finance", "etl", "banking", "transactions", "cli"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business :: Financial",
]
authors = [
    { name = "eschmidt42", email = "11818904+eschmidt42@users.noreply.github.com" }
]
requires-python = ">=3.13, <3.14"
dependencies = [
    "aiohttp>=3.13.4",
    "altair>=6.0.0",
    "beautifulsoup4>=4.14.3",
    "chardet>=7.3.0",
    "httpx>=0.28.1",
    "instructor>=1.14.5",
    "polars>=1.39.3",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.13.1",
    "pygments>=2.20.0",
    "python-dateutil>=2.9.0.post0",
    "rich>=14.3.3",
    "textual>=8.1.1",
    "urllib3>=2.7.0",
    "xlsxwriter>=3.2.9",
]

[project.urls]
Homepage = "https://github.com/eschmidt42/fintl"
Repository = "https://github.com/eschmidt42/fintl"
Issues = "https://github.com/eschmidt42/fintl/issues"

[project.scripts]
fintl = "fintl.cli.main:app"

[build-system]
requires = ["uv_build>=0.6.6,<0.11"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "inline-snapshot>=0.32.5",
    "jupyter-server>=2.18.0",
    "mistune>=3.2.1",
    "nbconvert>=7.17.1",
    "notebook>=7.5.6",
    "prek>=0.3.9",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "pytest-xdist>=3.8.0",
    "ruff>=0.15.7",
    "ty>=0.0.25",
    "vulture>=2.16",
]

[tool.pytest.ini_options]
markers = [
    "ollama: requires a running Ollama instance with a multimodal model (skipped by default; run with -m ollama)",
]
testpaths = ["tests"]

[tool.inline-snapshot]
format-command="ruff check --fix-only --stdin-filename {filename} | ruff format --stdin-filename {filename}"

[tool.ruff]
# https://docs.astral.sh/ruff/rules/
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".ipynb_checkpoints",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pyenv",
    ".pytest_cache",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    ".vscode",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "site-packages",
    "venv",
]

line-length = 100
indent-width = 4
target-version = "py313"

[tool.ruff.lint]
# https://docs.astral.sh/ruff/linter/#rule-selection
# https://docs.astral.sh/ruff/settings/#lintpydocstyle
select = [
    "I", # https://docs.astral.sh/ruff/rules/#isort-i
    "F", # https://docs.astral.sh/ruff/rules/#pyflakes-f
    "E", # https://docs.astral.sh/ruff/rules/#error-e
    "D", # https://docs.astral.sh/ruff/rules/#pydocstyle-d
    "C90", # https://docs.astral.sh/ruff/rules/#mccabe-c90
    "N", # https://docs.astral.sh/ruff/rules/#pep8-naming-n
    "A", # https://docs.astral.sh/ruff/rules/#flake8-builtins-a
    "C4", # https://docs.astral.sh/ruff/rules/#flake8-comprehensions-c4
    "LOG" # https://docs.astral.sh/ruff/rules/#flake8-logging-log
]

# On top of the Google convention, disable `D417`, which requires
# documentation for every function parameter.
ignore = ["D417"]

fixable = ["ALL"]

[tool.ruff.format]
# Like Black, use double quotes for strings.
quote-style = "double"

# Like Black, indent with spaces, rather than tabs.
indent-style = "space"

# Like Black, respect magic trailing commas.
skip-magic-trailing-comma = false

# Like Black, automatically detect the appropriate line ending.
line-ending = "auto"

# Enable auto-formatting of code examples in docstrings. Markdown,
# reStructuredText code/literal blocks and doctests are all supported.
#
# This is currently disabled by default, but it is planned for this
# to be opt-out in the future.
docstring-code-format = false

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.uv]
exclude-newer = "1 week"
exclude-newer-package = { "fintl" = false } # exempt package so the testpypi sanity check works

[tool.uv.audit]
ignore-until-fixed = ["GHSA-w8v5-vhqr-4h9v", "GHSA-hg6j-4rv6-33pg", "GHSA-jg22-mg44-37j8"]
